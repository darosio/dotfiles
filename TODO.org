For Calibre just remember: /Sync/media/books

* DONE Complete yazi setup [4/4]
- [X] compress / atool
- [X] epub (preview)
- [X] attach file and send mail
- [X] =, ,= for =, M=

* TODO Repo maintenance [0/12]
- [ ] [installer-integration-tests] Add temp-HOME smoke tests for =install.sh= and representative =*.stow.sh= packages; verify symlink creation and idempotent behavior.
- [ ] [declarative-host-profiles] Add declarative install profiles (desktop/laptop/server/ai) with package metadata such as =needs_root=, =depends_on=, and platform constraints.
- [ ] [extract-host-overrides] Move hardcoded personal paths and host assumptions into a local override layer or environment-driven settings.
- [ ] [fix-install-cwd] Update =install.sh= so scripts run from the dotfiles repo root; this fixes relative-path and =stow= package resolution issues.
- [ ] [fix-latex-package-install] Fix =latex.sh= so =texlive-bibtexextra= is actually passed to =yay= instead of being swallowed by an inline comment.
- [ ] [fix-ollama-relative-cd] Harden =2root.ollama.sh=: replace invalid top-level =return= with =exit 1= and anchor relative =cd= calls to the script or repo root.
- [ ] [remove-mu-premature-sync] Stop =mu.stow.sh= from running =mbsync= /= mu init= before the OAuth token exists; keep those as manual post-setup steps.
- [ ] [make-root-scripts-idempotent] Make rerunnable root scripts idempotent; avoid repeated =tee -a= appends in mdadm/rkhunter-style configs.
- [ ] [tighten-smartd-log-perms] Replace world-writable =/var/log/smartdnotify.log= permissions with least-privilege ownership and mode.
- [ ] [fail-fast-ai-container-secrets] Make =ai-containers.stow.sh= fail when =pass ai/khoj= is missing instead of writing an empty =.env=.
- [ ] [fix-zotero-relative-copy] Fix =zotero.stow.sh= relative copy paths and make the addon download idempotent.
- [ ] [hermes-botfather-token] Rotate the Telegram bot token via BotFather and store it as =pass cloud/telegram_bot_token=; the value there and the one in the real =~/.hermes/.env= already differ, and the latter leaked into a chat transcript. Then delete that file so =stow -t ~ agents= succeeds: stow is all-or-nothing, and that one real file is why =~/.local/bin/hermes-secrets= (the =secrets.command= helper Hermes reads, so Hermes starts with no secrets at all) and =~/.claude/setup-mcp-servers.sh= are absent from $HOME. Run =stow-audit= to confirm =agents= reports clean afterwards.

* calendar and orgzly
- [ ] define and save appropriate orgzly setting (e.le.2d or s.today)
  ad.2 o.e
- [ ] archive (or not archive) past events automatically
- [ ] capture in calfw
- [ ] links


* In general store:

#+begin_example
.local/share/applications/***.desktop
.config/***
#+end_example

* ApE
:PROPERTIES:
:CUSTOM_ID: ape
:END:
.ApE/ApE_Defaults.txt .ApE/DNA_ladders.txt

* pcmanfm
:PROPERTIES:
:CUSTOM_ID: pcmanfm
:END:
.config/libfm/libfm.conf

* thunderbird
:PROPERTIES:
:CUSTOM_ID: thunderbird
:END:
.thunderbird/

* VirtualBox
:PROPERTIES:
:CUSTOM_ID: virtualbox
:END:
.VirtualBox VMs/XP32-2015/XP32-2015.vbox .VirtualBox
VMs/XP32-2015/XP32-2015.vbox-prev .VirtualBox
VMs/XP32-MO2007_Origin7/XP32-MO2007_Origin7.vbox .VirtualBox
VMs/XP32-MO2007_Origin7/XP32-MO2007_Origin7.vbox-prev .VirtualBox
VMs/old64/old64.vbox .VirtualBox VMs/old64/old64.vbox-prev
.config/VirtualBox/VirtualBox.xml .config/VirtualBox/VirtualBox.xml-prev
.config/VirtualBox/compreg.dat .config/VirtualBox/xpti.dat

** and ignore:
:PROPERTIES:
:CUSTOM_ID: and-ignore
:END:
.config/VirtualBox//.log .config/VirtualBox//.log._ .VirtualBox\
VMs/_/Logs/*

*   emacs config
Here is a revision that simplifies your init.el by removing redundant or less-used packages, consolidates similar functionality, and transitions to some better-maintained or recommended alternatives. The goal is to keep your core workflow intact but reduce complexity and maintenance effort:

  ;; ---


  ;; - For spell/translation: keep only essential `flyspell`, `consult-flyspell`, `guess-language`.
  ;; - For completion, rely on `vertico`, `orderless`, `consult`, `embark` — already well set up.

  ;; ### Recommended transitions

  ;; - For snippet support: continue with `yasnippet` and `yasnippet-snippets` (well maintained).
  ;; - For LSP: use `eglot` (which you have) but add minimal configuration only.
  ;; - Use `apheleia` for formatters, keep it tuned to your existing setup.
  ;; - Use `doom-modeline` + `nerd-icons` + `minions` as your modeline stack.
