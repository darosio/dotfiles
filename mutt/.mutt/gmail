set realname="Daniele P. Arosio"
set edit_headers=yes
set sendmail  = "/usr/bin/msmtp -a gmail"
set signature="~/.mutt/gmail.signature"
set mbox      = "+gmail/archive"
# Specify where to save and/or look for postponed messages.
set postponed = +gmail/draft
unset record

color status cyan default

#set folder=$HOME/.mail/gmail
#mailboxes ! + `\
#for file in ~/.maildir/gmail/*; do \
#  box=$(basename "$file"); \
#  if [ ! "$box" = '.' -a ! "$box" = '..' -a ! "$box" = '.customflags' \
#	  -a ! "$box" = '.subscriptions' ]; then \
#	echo -n "\"+gmail/$box\" "; \
#  fi; \
#done`

mailboxes ! + `echo -n "+ "; find ~/.maildir/gmail/ -maxdepth 1 -type d -name "*" -printf "+'%f' "`

## Deleto to trash and move into Spam
macro index D \
    "<save-message>+gmail/trash<enter>"  "move message to the trash"
macro index S \
    "<save-message>+gmail/spam<enter>"   "mark message as spam"
